default: &default
  adapter: <%= ENV['glyph_DATABASE_ADAPTER'].presence || 'mysql2' %>
  encoding: <%= ENV['glyph_DATABASE_ENCODING'].presence || 'utf8' %>
  pool: <%= ENV['glyph_DATABASE_POOL'].presence || ENV.fetch('RAILS_MAX_THREADS') { 10 } %>
  reconnect: <%= ENV['glyph_DATABASE_RECONNECT'].presence || 'true' %>
  username: <%= ENV['glyph_DATABASE_USERNAME'].presence || 'glyph' %>
  password: <%= ENV['glyph_DATABASE_PASSWORD'] %>

development:
  <<: *default
  database: <%= ENV['glyph_DATABASE_NAME'].presence || 'glyph_development' %>

test:
  <<: *default
  database: <%= ENV['glyph_DATABASE_NAME'].presence || 'glyph_test' %>

production:
  <<: *default
  database: <%= ENV['glyph_DATABASE_NAME'].presence || 'glyph_production' %>
  host: <%= ENV['glyph_DATABASE_HOST'] || '' %>
  port: <%= ENV['glyph_DATABASE_PORT'] || '' %>
