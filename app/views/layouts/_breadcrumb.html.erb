<%= react_component('layout/Breadcrumb', { playbook: (defined?(playbook) ? playbook : ''), crumbs: crumbs, jurisdiction: jurisdiction_path.join(', '), isolation: (defined?(isolation) && isolation) || false, enroller: current_user.role?(Roles::ENROLLER) }) %>
